<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="/">BitGo</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <%if (user) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/user/profile">Profile <span class="sr-only">(current)</span></a>
                </li>
            <% } %>
        </ul>
        <ul class="navbar-nav ml-auto">
            <%if (!user) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/user/login">Login</a>
                </li>
            <% } %>
            <%if (user) { %>
                <li class="nav-item">
                    <form action="/user/logout" method="POST">
                        <button type="submit" class="btn btn-outline-secondary my-2 my-sm-0" href="/user/logout">Logout</button>
                    </form>
                </li>
            <% } %>
        </ul>
    </div>
</nav>